export const meta = {"name": "tare-rush-2", "description": "Vague 2 : graphe, compteur HCS, assistant, garde, livraison", "phases": [{"title": "Build2", "detail": "5 lots en parallele"}]}

const CTX = "CONTEXTE. ETHOnline 2026, build jusqu au 11 sept, soumission le 13. Dev solo, GROS RUSH en cours.\nLe projet TARE est DEJA DEBOUT dans . : moteur verifie, API sur :8787, web, landing, MCP.\n115+ tests verts. `make test` et `make gate-a3` passent. NE CASSE RIEN de ce qui existe.\n\nTARE mesure ce qu un hook Uniswap v4 prend sur un swap : sur un fork epingle, anvil_setCode remplace\nle bytecode du hook par un stub inerte de 89 octets conforme a Hooks.sol, on cote le meme swap deux\nfois, l ecart EST le prelevement.\n\nDONNEES REELLES DISPONIBLES : docs/dataset/measurements.jsonl = 995 mesures, 199 pools,\n12 hooks, bloc 50614000 Base. 720 MEASURED, 265 NOT_QUOTABLE, 10 NOT_MEASURABLE (comptabilite\npersonnalisee). 545 mesures > 1 bps sur des pools a stored_lp_fee ZERO, sur 109 pools et 6 hooks.\n63 profils non plats, le plus marque va de 689,95 a 406,64 bps. Max observe 1176,46 bps.\ndocs/dataset/summary.json et docs/hooklist.json (613 fiches du registre officiel).\n\nREGLE D EXPLOITATION CRITIQUE : UN SEUL MESUREUR PAR ANVIL. measure() ecrit le stub puis le restaure,\nc est de l etat global du noeud. Deux processus sur le meme fork -> l un restaure le stub de l autre\net un hook reste stubbe en permanence. Si tu mesures, prends TON PROPRE port anvil.\n\nREGLES DURES DU PRODUIT :\n1. Le modele ne produit JAMAIS un nombre. Il choisit quoi interroger et explique ce qui revient.\n2. Etiquettes : MEASURED | INTERPOLATED | NOT_MEASURABLE | NOT_QUOTABLE. Jamais promues.\n3. NE JAMAIS conclure sur une reponse tronquee : ce projet a produit CINQ faux resultats comme ca.\n   Une lecture bornee, un timeout, un rate-limit -> NOT_MEASURABLE, jamais une valeur, jamais un zero.\n4. Chaque valeur porte son bloc, sa taille, son sens, et se rejoue en une commande.\n\nCODE A REUTILISER (lecture seule hors de .) :\n  CorLens v2 : <projet-local-anterieur-B> \u2014 apps/ai-service (embedding, completion, HMAC, prompt-log, usage),\n    apps/mcp-server, apps/path (RAG pgvector qui MARCHE), packages/contracts (Zod).\n    BUG A NE PAS HERITER : apps/corridor/src/app.ts:118 fait `void ragIndex;` \u2014 l index n est jamais peuple.\n  COBOL Explorer : <projet-local-anterieur-A> \u2014 ingestion/graph/{build,queries,backend}.py (networkx MultiDiGraph,\n    impact/lineage/orphans/callers/callees), ingestion/index/{embed,load_pgvector}.py, server/agent/.\n    BUG A NE PAS HERITER : server/api/app.py:157 reconstruit GraphTools a chaque requete HTTP\n    (102 ms de CPU brules par appel pour une requete a 0,09 ms). METS LE GRAPHE EN CACHE.\n  LPLens : <projet-local-anterieur-C> \u2014 phases typees, etiquettes\n    d honnetete. NE REPRENDS PAS 06-scoring/scoreHook.ts (multiplicateurs en dur, non calibres).\n\nSPONSORS : Uniswap (le sujet), Hedera (le guichet x402, hedera:testnet via api.testnet.blocky402.com,\npayeur 0.0.10367920, encaisseur 0.0.10367997, feePayer facilitateur 0.0.7162784), Ledger (la garde, conditionnel).\nLe .env de . contient tout. Lis-le, ne l affiche jamais, ne le commite jamais.\n\nREGLES : reponds en francais. Ecris du VRAI code teste, pas un squelette. Commite ton lot.\nNe touche QU AU repertoire assigne. Si tu n as pas verifie, ecris NON VERIFIE."
const S = {"type": "object", "additionalProperties": false, "required": ["lot", "fait", "commande_de_preuve", "resultat_obtenu", "reste_a_faire", "non_verifie"], "properties": {"lot": {"type": "string"}, "fait": {"type": "string"}, "commande_de_preuve": {"type": "string"}, "resultat_obtenu": {"type": "string"}, "reste_a_faire": {"type": "string"}, "non_verifie": {"type": "string"}}}
const L = [{"k": "graphe", "path": "engine/tare/graph/", "p": "LOT F \u2014 LE GRAPHE. Porte le graphe de COBOL Explorer sur les donnees TARE.\na) Lis <projet-local-anterieur-A>/ingestion/graph/build.py et queries.py. Reprends la structure networkx.MultiDiGraph\n   avec UNE arete par (src,dst,kind) et la fusion d evidence, et les primitives _in_of/_out_of.\nb) Schema TARE \u2014 noeuds : Hook, Bytecode, Pool, Token, Deployer, Measurement, RegistryEntry.\n   Aretes : ATTACHED_TO (Hook->Pool), HOLDS (Pool->Token), DEPLOYED_BY, HAS_BYTECODE,\n   MEASURED_AS (POOL->Measurement, PAS Hook->Measurement : la these est que le prelevement est une\n   propriete du COUPLE), LISTED_IN (Hook->RegistryEntry).\n   RegistryEntry est un NOEUD, pas des proprietes du Hook : 37 couples (adresse,chainId) portent DEUX\n   fiches dont 10 aux flags contradictoires. Bytecode est un noeud EN ETOILE, pas des aretes en clique\n   (mesure : 356 ms en clique contre 10 ms pre-agrege).\nc) Requetes : impact(hook) le rayon de souffle, twins(hook) les clones par bytecode, deployer_cluster(hook),\n   orphans() les hooks du registre sans pool liquide, contradictions() les hooks a deux fiches divergentes,\n   disagreement() ceux que le registre dit vanillaSwap=false mais que la mesure donne a ~0 bps.\nd) Construis-le pour de vrai depuis docs/dataset/measurements.jsonl + docs/hooklist.json. Recupere les\n   bytecodes et les deployeurs par RPC (eth_getCode, et la tx de creation via le mirror ou eth_getLogs).\n   Serialise en JSON, et METS-LE EN CACHE au chargement (le bug de COBOL Explorer).\ne) Tests : au moins 10. Publie les chiffres reels que le graphe revele (combien de clones, etc.)."}, {"k": "metering", "path": "apps/api/src/metering/", "p": "LOT G \u2014 LE COMPTEUR ET LE JOURNAL HCS. C est l extra point Hedera, mot pour mot :\n'Pay-per-call inference, data, or compute metering rather than a flat per-request charge'.\na) Porte la comptabilite d usage de CorLens : <projet-local-anterieur-B>/apps/ai-service/src/controllers/usage.controller.ts\n   et services/usage.service.ts et repositories/prompt-log.repo.ts. Adapte : l unite facturee n est pas\n   la requete, c est LA MESURE (une paire de cotations). Une requete qui demande 5 tailles x 2 sens = 10 mesures.\nb) Porte aussi le middleware HMAC : <projet-local-anterieur-B>/apps/ai-service/src/middleware/hmac-verify.ts.\nc) LE JOURNAL HCS : chaque lot de mesures publie une empreinte sur un topic HCS Hedera testnet\n   ('Verifiable payment audit trails on HCS', autre extra point). Cree le topic, publie, et VERIFIE\n   la lecture par le mirror node https://testnet.mirrornode.hedera.com/api/v1/topics/{id}/messages.\n   Donne le topic id reel et le cout reel par message.\nd) Expose GET /usage : total de mesures facturees, par payeur, avec les hashes de reglement.\ne) Tests : au moins 8. Et un vrai message HCS publie, dont tu donnes le lien HashScan."}, {"k": "assistant", "path": "apps/api/src/assistant/", "p": "LOT H \u2014 L ASSISTANT QUI PILOTE LE FRONT. Le differenciateur du projet.\na) Il ne rend pas du texte : il rend des ACTIONS TYPEES validees par Zod, que le front execute.\n   Commandes : filter({minBps,chain,flag,label,allowlisted,registryDisagrees}), sort(col,dir),\n   highlight([hooks]), open(hook,pool), plotCurve(hook,pool), compare(A,B), measure(...),\n   showTwins(hook), showBlastRadius(hook), showDeployer(hook), showContradictions(), showOrphans(),\n   reset(), columns([...]), showEvidence(measurementId), clarify(question), export(format), permalink().\nb) Boucle : question -> planificateur -> graphe (structure, LOT F) + JSONL (grandeurs) -> resultats\n   -> narration + actions. Flux SSE.\nc) REGLE D OR, a ecrire dans le code et le README : le modele choisit quoi interroger et explique ce\n   qui revient, IL NE PRODUIT JAMAIS UN NOMBRE. Toute valeur citee doit provenir d une ligne du dataset,\n   avec son bloc, sa taille, son sens.\nd) CONTRAINTE VERIFIEE : `measure` facturee en x402 depuis le navigateur est IMPOSSIBLE, aucun visiteur\n   n a de compte Hedera. x402 garde l API et le MCP ; le chat a un quota par session.\ne) Le moment de la demo : 'montre-moi les hooks que le registre dit vanillaSwap=false mais qui mesurent\n   moins de 1 bps' -> le tableau se reduit -> on ouvre une ligne -> la courbe se dessine. Fais que ca marche.\nf) Tests : au moins 10, dont un qui prouve que le modele ne peut pas inventer un nombre."}, {"k": "garde", "path": "packages/guard/", "p": "LOT I \u2014 LA GARDE. C est L ACTION, et elle corrige notre axe le plus faible :\n0 des 27 finalistes async d ETHGlobal est un instrument de lecture seule, 27 sur 27 livrent une action.\na) Un paquet npm @tare/guard : `tareGuard(txRequest)` intercepte un swap v4 AVANT signature, decode\n   la PoolKey dans le calldata pour en extraire l adresse du hook, consulte la table pre-calculee,\n   et renvoie un verdict : { hook, bps, size, block, label, verdict: 'ok'|'warn'|'block' }.\n   VERIFIE : mesurer en direct est impossible (9 s a froid) -> c est une CONSULTATION, pas une mesure.\n   Le hook est lisible aux octets 0xa0..0xc0 de params[j] du calldata Universal Router.\nb) Une extension navigateur minimale (manifest v3) qui fait la meme chose sur window.ethereum :\n   interception EIP-1193 + EIP-6963, piege Object.defineProperty pour les providers tardifs\n   (patron Magnee, 3 prix a HackMoney). Seul eth_sendTransaction est intercepte, le reste passe.\n   Affiche : 'ce hook prend N bps a ta taille, mesure au bloc B. Continuer ?'\nc) Un exemple qui marche : une page de test qui declenche un faux swap et montre la garde s ouvrir.\nd) Prevois le point de branchement Ledger (le device approuve) sans le cabler : une interface\n   `approver` avec une implementation 'confirm()' par defaut.\ne) Tests : au moins 10, dont le decodage de calldata sur des exemples reels."}, {"k": "livraison", "path": "docs/ + FEEDBACK.md + PR/", "p": "LOT J \u2014 LA LIVRAISON. Les cases eliminatoires et le contenu de soumission.\na) FEEDBACK.md a la racine du depot : le retour developpeur sur la stack Uniswap. C est ELIMINATOIRE\n   pour le prix Uniswap ('A public GitHub repository with open-source code, a FEEDBACK.md file, and a\n   completed submission to the Uniswap Developer Feedback Form that includes the link to your FEEDBACK.md').\n   Ecris un vrai retour, precis et utile : ce qui a bloque (le quoter qui ne cote que dans un sens,\n   NotEnoughLiquidity enveloppe dans UnexpectedRevertBytes, l absence de champ numerique au registre,\n   verifiedSource qui rend le registre aveugle a 97% des hooks, l allowlist du routeur ecrite a la main\n   avec une denylist vide). Cite les fichiers et les lignes.\nb) LA PR SUR LE SCHEMA hooklist : prepare, dans docs/pr-hooklist/, le patch qui ajoute un champ\n   numerique au schema d Uniswap (repo Uniswap/hooklist, schema.json a additionalProperties:false en 4\n   endroits, donc le champ DOIT etre declare). Propose `measuredExtraction: {bps, block, method, sourceUrl}`.\n   Ecris le diff, le corps de la PR, et la justification chiffree.\nc) Le README principal : verifie que chaque affirmation pointe vers un fichier et une ligne\n   ('Make sure your README clearly points to the relevant contracts and lines of code so we can verify\n   your integration'). Ajoute la section AI attribution et le compte de tests.\nd) docs/METHOD.md : la methode, ses limites, et les CINQ faux resultats que ce projet a produits et\n   corriges (troncature [:3], corps a 2000 octets, head -c 220, rate-limit pris pour un zero, deux\n   mesureurs sur un fork). C est le signal de serieux le plus fort du depot.\ne) docs/HONESTY.md : les 4 etiquettes, ce que chacune veut dire, et ce qu on refuse d affirmer."}]

phase('Build2')
const r = (await parallel(L.map(l => () =>
  agent(CTX + '\n\nTON LOT (ne touche a rien d autre) : ' + l.path + '\n\n' + l.p,
        { label: 'lot:' + l.k, phase: 'Build2', schema: S, effort: 'high' })
))).filter(Boolean)
log('vague 2 : ' + r.length + '/5')
return { lots: r }
